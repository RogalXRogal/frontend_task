import React from "react";
import logo from "./Assets/LOGO.svg"
const Header = () => {
    return(
        <header>
            <img src={logo} alt="logo"  />
        </header>
    );
}

export default Header;